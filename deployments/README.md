# Deployment workspace

The v0.1 reference deployment will use an unpublished origin network, a
separate ingest network, and an executor that shares only the Gateway network
namespace. Deployment manifests must preserve the capability and reachability
matrix in `docs/ADR.md`; this directory is not evidence of a verified runtime.
