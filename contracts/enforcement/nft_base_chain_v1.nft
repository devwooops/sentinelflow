table inet sentinelflow {
  set blacklist_ipv4 {
    type ipv4_addr
    flags timeout
  }

  chain gateway_input {
    type filter hook input priority 0
    policy accept
    tcp dport 8080 ip saddr @blacklist_ipv4 drop
  }
}
