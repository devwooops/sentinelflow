# Sample workspace

Only synthetic, documentation-safe inputs belong here. Real credentials,
traffic captures, authentication data, raw request paths, and production logs
must not be committed.

`tuning/threshold_cases_v1.json` contains only synthetic aggregate counts and
closed explanation codes. It intentionally contains no source address,
account hash, request data, actor identity, or production-derived baseline.
